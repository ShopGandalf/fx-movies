Write tests for the code changes or files specified by the user.

Follow the existing test conventions in this codebase:
- Match the test file location and naming convention of existing tests
- Use the same test framework/libraries already in use
- Cover the happy path, edge cases, and error scenarios
- Keep tests focused and independent — no shared mutable state

Run the tests after writing them to confirm they pass.
